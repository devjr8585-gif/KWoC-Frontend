pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'build the app'
            }
        }
        stage('test') {
            steps{
                echo 'test the app'
            }
        }
        stage('deploy')
            steps{
                echo 'deploy the app'
            }
        }
    }
}
